[function]
name = "hello-go"
language = "go"
entrypoint = "handler.go"

[runtime]
port = 0                  # Auto-assign
health_path = "/health"

[build]
base_image = ""           # Default: golang:1.26-alpine3.23
runtime_image = ""        # Default: alpine:3.23

[env]
# MY_VAR = "value"

[dependencies]
packages = []
# Example with dependencies:
# packages = ["github.com/fatih/color@v1.18.0"]
